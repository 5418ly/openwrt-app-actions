# apps/.github/workflows/mvebu.env
# OpenWrt 23.05.3, for mvebu/cortexa9 (e.g., Linksys WRT系列路由器)

# 替换为你实际的 OpenWrt 版本和对应的SDK下载路径
SDK_URL=https://downloads.openwrt.org/releases/24.10.0/targets/mvebu/cortexa9/

# 替换为你实际的SDK文件名（不含 .tar.xz 后缀）。
# 注意：文件名中的gcc版本和libc类型（musl/glibc）可能会因OpenWrt版本而异，请务必核对。
SDK_NAME=openwrt-sdk-24.10.0-mvebu-cortexa9_gcc-13.3.0_musl_eabi.Linux-x86_64

# 这是编译后IPK包所在的目录名，通常是 target_subtarget 形式
SDK_ARCH=mvebu_cortexa9
